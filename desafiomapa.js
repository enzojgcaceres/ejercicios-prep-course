// Ejercicio 1: Map
// Escribe una función que tome un arreglo de números como entrada y devuelva un nuevo arreglo con cada número
// multiplicado por 2.

let nuevoArreglo = numeros.map(function (numero) {
    return numero * 2;
})

// let nuevoArreglo = numeros.map(numero => numero * 2); // otra version con funcion de flecha.
