// Ejercicio 2: Filter
// Escribe una función que tome un arreglo de números como entrada y devuelva un nuevo arreglo que
// contenga solo los números pares.

let arrayDePares = arrayDeNumeros.filter( function (numero) {
    return numero % 2 === 0;
})

 // let arrayDePares = arrayDeNumeros.filter(numero => numero % 2 === 0);
